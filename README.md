# Semaphore Dockerized

## .env-example
Copy .env-example to .env and fill in the fields to manage your own setup

## Data folder
The database (Mysql or Postgres) gets saved to the .data folder which should be ignored by git
